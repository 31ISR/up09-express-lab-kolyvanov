info:
  name: user_login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}auth/login"
  body:
    type: json
    data: |-
      {
          "username": "admin",
          "password": "qwerty123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
