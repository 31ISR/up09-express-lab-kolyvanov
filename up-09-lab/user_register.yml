info:
  name: user_register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}auth/register"
  body:
    type: json
    data: |
      {
          "username": "admin",
          "email": "admin@example.com",
          "password": "qwerty123",
          "role": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
