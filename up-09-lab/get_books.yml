info:
  name: get_books
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}api/books?author=Джоан Роулинг"
  params:
    - name: author
      value: Джоан Роулинг
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
