info:
  name: get_books
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}api/books"
  body:
    type: json
    data: |-
      {
        "author": "Джордж Оруэл",
        "genre": "антиутопия"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
